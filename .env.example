# Copy to .env in the repository root. CLI and WebUI load it automatically.
# Existing environment variables take precedence. Values are not interpolated.
OPENAI_API_KEY=replace-with-company-key
OPENAI_BASE_URL=https://your-provider.example/v1
OPENAI_MODEL=replace-with-tool-capable-model

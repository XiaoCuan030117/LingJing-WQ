# 最小 Coding Agent 实施计划

## 目标与范围

依据 `TASK.md`，先完成并提交阶段一最小 Coding Agent，再实现阶段二方向一：Streamlit WebUI、工具权限控制和 `ask_user`。目标是在约 180 分钟内完成可演示、可测试的完整闭环，优先保证已选功能的完成度。

当前仓库只有 `TASK.md`、`AGENTS.md` 和 `.gitignore`，尚无代码或运行配置。本文件仅规划后续实施，不代表功能已经完成。

- 使用 Python、Streamlit、与公司 API 兼容的直接模型 SDK；启动前确认 API 地址、模型名称及工具调用能力。
- 用普通 Python 函数实现串行 agent loop，不引入 Agent 框架、独立后端服务、数据库、消息队列或账户系统。**虽然 `TASK.md` 允许使用任意框架，本实现选择原生 Python 以保证透明度和可控性，便于演示和理解核心逻辑。**
- 面向本机单用户演示；不实现工具并行、联网检索、长期记忆、部署平台或完整沙箱。
- 阶段一用 CLI 独立验收，阶段二复用同一核心。开发全程录屏；API Key 通过环境变量提供，不进入代码或录屏。

## 核心结构与契约

建议文件布局：`src/agent.py` 管理循环与状态，`src/tools.py` 执行工具，`src/model.py` 隔离模型配置，`src/cli.py` 提供阶段一入口，`app.py` 提供 Streamlit 界面，`tests/` 存放测试。交付 `requirements.txt`、格式检查配置、`.env.example` 和 `README.md`，以实际可运行命令为准。

### 输入与输出

| 入口或工具 | 输入 | 输出 |
| --- | --- | --- |
| 用户任务 | 自然语言、预先配置的工作目录 | 最终回答，或等待授权／补充信息的界面 |
| `read_file` | 工作目录内文件路径 | UTF-8 文本或结构化错误 |
| `write_file` | 路径、完整文本内容 | 写入结果或结构化错误；覆盖须明确展示 |
| `run_shell` | 命令字符串 | 标准输出、标准错误、退出码或超时信息 |
| `ask_user` | 一个清晰问题 | 用户提交的文本回答；提交前暂停循环 |

工具结果统一包含成功标记、结果或错误类型及说明，并关联原工具调用 ID。模型请求同时携带历史消息和已完成的工具结果。工具参数先校验再执行，未知工具不得执行。

Shell 的解释器和工作目录固定于启动配置并写入 README；当前 Windows 环境优先使用 PowerShell。文件工具解析路径后限定在工作目录内，拒绝绝对路径越界和 `..` 越界。Shell 工作目录不等于安全沙箱，命令可能访问目录外资源，因此所有 Shell 调用均需批准。

### 核心状态与状态迁移

| 状态 | 含义与后续动作 |
| --- | --- |
| `idle` | 接收新任务，进入 `running` |
| `running` | 请求模型，串行处理工具；无工具调用的最终回答进入 `completed` |
| `waiting_approval` | 保存待执行调用，等待允许或拒绝；处理决定后回到 `running` |
| `waiting_user` | 保存问题及调用 ID；提交非空回答后回到 `running` |
| `completed` | 展示最终结果，允许下一轮对话 |
| `failed` | 展示终止原因，允许重置或发起新任务 |

会话至少保存：消息历史、运行 ID、当前状态、待处理工具队列、当前等待项、工具结果及每轮步数。一次模型响应包含多个调用时按顺序处理，遇到授权或提问立即暂停，恢复后继续剩余调用。

Streamlit 使用 `st.session_state` 保存会话。按钮或表单提交只消费一次等待项；已完成的调用不得因 rerun 重复执行。等待期间禁止提交新任务覆盖当前任务；不自动轮询模型。进程退出或会话丢失后不保证恢复，也不自动重放操作。

### 权限与失败路径

- 目录内读取自动执行；所有写文件和 Shell 命令逐次授权，不提供永久允许。权限由工具分发层强制执行，不能只依靠提示词。
- 授权界面展示工具名、完整命令或目标路径与待写内容、是否覆盖。批准仅绑定当前调用的确切参数；拒绝时不产生副作用，将拒绝结果返回模型。
- `ask_user` 无须授权；不以空文本代替回答，不在等待期间继续调用模型。
- 文件不存在、编码错误、参数错误、越界、命令非零退出均作为工具结果返回，模型可据此修正；不能伪报成功。
- 模型认证、网络或响应格式错误终止本轮并展示可操作说明；不自动重试有副作用的工具。
- 初始上限：每轮最多 12 次模型请求、20 次工具调用；模型请求超时 60 秒，Shell 超时 30 秒；文本读取和写入上限各 100 KB，Shell 输出保留前 20 KB 并标明截断。达到上限应明确结束，避免无限循环。
- Shell 超时尝试终止本次进程并报告结果；演示范围不支持后台或交互式命令，不宣称实现完整进程隔离。

## 分阶段实施与独立验收

以下时间包含该阶段验证，总计约 180 分钟（含 15 分钟机动缓冲）。若时间不足，先减少界面装饰和额外示例，保留三项已选扩展及核心失败路径验证。

**二阶段方向选择说明**：依据 `TASK.md`，本次选择**方向一**进行扩展，具体实现以下三项可选改进：
1. ✅ 基础对话功能的 WebUI（Streamlit）
2. ❌ 工具并行调用（不实现，保持串行以简化状态管理）
3. ✅ 工具权限控制（危险工具需用户同意）
4. ✅ ask_user 工具（Agent 主动提问并等待回答）

未选择方向二（hiring-demo 前端项目）。

### 第一阶段：最小 Agent 与 CLI（约 60 分钟）

**目标**：独立完成题目阶段一，建立任务→模型→工具→模型→结果闭环。

**交付物**：模型适配、三个基础工具、串行循环、CLI、配置示例和初版启动说明。核心先提供可暂停的调用分发接口；CLI 对写入和 Shell 提供简单逐次确认，供安全验收使用。

**验证方式**：在临时工作目录中，真实模型完成文件总结、按需求生成脚本并运行；核对生成文件与实际命令输出。用模拟模型验证多步工具调用、失败回传和步数上限。无有效 API Key 时，只能记录模拟验证通过，不能宣称真实闭环已验收。

**阶段门槛**：测试通过、CLI 演示完成后提交独立 commit，再开始第二阶段，满足 `TASK.md` 的阶段提交要求。

### 第二阶段：Streamlit 对话与工具授权（约 45 分钟）

**目标**：复用核心，在网页完成对话与危险操作审批。

**交付物**：聊天输入、历史消息、工具执行摘要、状态提示、允许／拒绝控件；将 CLI 的暂停接口接入 `session_state`。依赖就绪后的启动命令为 `streamlit run app.py`。

**验证方式**：网页可总结文件；写入或运行命令前保持等待。批准后执行一次，拒绝后文件和命令均不产生副作用且 Agent 能继续解释结果。触发页面 rerun，确认不重复执行、不丢失当前审批。测试同一响应中的多个工具依次暂停和恢复。

### 第三阶段：ask_user 暂停与恢复（约 30 分钟）

**目标**：信息不足时由 Agent 主动提问，收到回答后继续原任务。

**交付物**：`ask_user` 工具定义、问题表单、回答回传及恢复逻辑；保留原任务上下文和待处理队列。

**验证方式**：用确定性的模拟模型触发“提问→回答→请求写入→批准→完成”；等待时模型请求计数不增加，空回答不恢复，重复提交不重复回传。真实模型使用缺少目标文件名的任务演示一次，确认回答实际用于后续操作。

### 第四阶段：回归与交付整理（约 45 分钟，含 15 分钟机动缓冲）

**目标**：验证整体闭环可复现并完成题目交付材料。

**交付物**：完整测试、README 中的安装／配置／启动／测试步骤、功能边界、阶段一 commit 标识及演示步骤；补齐 Python 生成物的忽略规则。整理录屏，将代码上传 GitHub并提供仓库地址。

**验证方式**：按 README 在新虚拟环境运行；执行 `python -m pytest` 和选定格式／静态检查命令。演示”提出脚本需求→回答补充问题→批准写入→拒绝一次执行→明确重新请求并批准→看到实际执行结果”，另演示文件总结。检查无密钥入库，录屏可访问，阶段一 commit 在扩展开发之前。

**时间分配说明**：四个阶段合计 180 分钟（60+45+30+45），第四阶段包含 15 分钟机动缓冲用于处理调试、录屏操作和意外问题。若前三阶段超时，优先保证核心功能验收通过，可适当简化界面细节和额外测试用例。

## 测试范围与完成定义

- **工具单测**：临时目录中的读写、覆盖、缺失文件、越界、编码和大小限制；无害 Shell 成功、非零退出、超时及输出截断。
- **核心单测**：模拟模型响应；覆盖正常结束、未知工具、非法参数、多个调用排队、调用 ID 对应、API 错误及循环上限。
- **交互状态测试**：批准执行一次、拒绝零副作用、等待不推进、回答恢复、重复事件和 rerun 不重放。优先测试核心状态逻辑，网页用手工复现清单验证，无须引入浏览器自动化服务。
- **真实冒烟测试**：仅用少量模型请求验证文件总结、脚本生成执行及提问授权闭环；日常测试不调用付费 API，不设虚构覆盖率指标。

完成条件是四个阶段的交付物和验证均有结果，三项已选扩展能够连贯运行，失败有明确反馈，README 可复现，录屏与 GitHub 地址可交付。美化、多用户、持久化和并行执行不影响本次验收。
